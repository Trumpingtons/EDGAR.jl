# EDGAR.jl

EDGAR.jl is a small Julia package to fetch and extract sections from SEC EDGAR filings.

Minimum Julia: **1.12**

Quick start

```bash
cd ~/EDGAR.jl
julia --project=. -e 'using Pkg; Pkg.instantiate()'
```

See the API page for exported functions and usage examples.
