# API

```@meta
CurrentModule = EDGAR
```

The exported `EDGAR.jl` functions, generated from their docstrings. Build the
docs locally with:

```bash
julia docs/make.jl
```

```@autodocs
Modules = [EDGAR]
Private = false
```
